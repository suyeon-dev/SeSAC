<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>방명록 페이지</title>
  </head>
  <body>
    <form name="visitor-form">
      <h1>방명록 페이지</h1>
      <fieldset>
        <legend>방명록 페이지</legend>
        <input type="text" name="name" placeholder="사용자 이름" />
        <input type="text" name="comment" placeholder="간단 방명록 작성" />
        <div>
          <button type="button">방명록 등록</button>
        </div>
      </fieldset>
    </form>

    <table border="1" callspacing="0">
      <thead>
        <th>ID</th>
        <th>작성자</th>
        <th>방명록</th>
        <th>수정</th>
        <th>삭제</th>
      </thead>
      <tbody>
        <!-- data:
            [
                { id: 1, name: '베베', comment: '어렵당' },
                { id: 2, name: '??', comment: 'DB가 절 때렸어요' },
            ] 
        -->
        <!-- Cvisitor에서 보내준 모델의 데이터 사용 -->
         <% for(let i=0;i<data.length;i++){ %>
            <tr>
                <td><%=data[i].id %></td>
                <td><%=data[i].name %></td>
                <td><%=data[i].comment %></td>
                <td><button>수정</button></td>
                <td><button>삭제</button></td>
            </tr>
         <% } %>
      </tbody>
    </table>
  </body>
</html>
